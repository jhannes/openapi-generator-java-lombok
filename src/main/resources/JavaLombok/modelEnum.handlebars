@Getter
@RequiredArgsConstructor
@ToString
public enum {{classname}} {
{{#allowableValues}}
{{#enumVars}}
{{#if enumDescription}}
    /**
     * {{enumDescription}}
     */
{{/if}}
    {{{name}}}({{{value}}}){{#if @last}};{{else}},{{/if}}
{{/enumVars}}
{{/allowableValues}}

    private final String name;
}